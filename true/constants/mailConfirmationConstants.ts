export const INPUT_MAIL_CONFIRMATION = document.querySelector(
  "#code-input"
) as HTMLInputElement;

export const BUTTON_LOGIN_MAIL_CONFIRMATION = document.querySelector(
  "#log-in"
) as HTMLButtonElement;

export const BUTTON_CLOSE_MAIL_CONFIRMATION = document.querySelector(
  "#close-mail-confirmation"
) as HTMLButtonElement;
