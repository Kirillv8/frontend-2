export const BUTTON_OPEN_SETTING_CHAT = document.querySelector(
  "#open-settings"
) as HTMLButtonElement;

export const BUTTON_EXIT_CHAT = document.querySelector(
  "#exit-button"
) as HTMLButtonElement;

export const INPUT_MESSAGE_FIELD = document.querySelector(
  "#message-input"
) as HTMLInputElement;

export const BUTTON_SEND_MESSAGE = document.querySelector(
  "#send-btn"
) as HTMLButtonElement;
