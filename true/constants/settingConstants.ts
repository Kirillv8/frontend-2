export const INPUT_CHANGE__NAME_SETTING = document.querySelector(
  "#change-name-input"
) as HTMLInputElement;

export const BUTTON_CHANGE_NAME_SETTING = document.querySelector(
  "#change-name"
) as HTMLButtonElement;

export const BUTTON_CLOSE_SETTING = document.querySelector(
  "#close-settings"
) as HTMLButtonElement;
